# Local-Proof-Gated Funding Plan

Status: estimates unresolved; no funding authorized.

No approved read-only RPC or bundler is configured, so O.19 has no live gas,
fee, prefund, balance, or funding result. The calculator requires:

- target deployment gas;
- factory deployment gas;
- counterfactual account verification gas;
- first action call gas;
- pre-verification gas;
- bounded maximum fee per gas.

It calculates separate deployer and account minimums, then applies a 25 percent
buffer. The recommended amount must remain under an independently approved
hard maximum. No EntryPoint deposit is required because there is no paymaster.
The counterfactual account must have sufficient prefund before its first
UserOperation unless a later reviewed execution design provides it atomically.

Human checklist:

- use a disposable Sepolia-only deployer and funder;
- use no seed phrase generated by repository tooling;
- place no meaningful asset in either address;
- independently approve whether deployer and funder may be the same address;
- record live gas/fee evidence and maximum exposure before funding;
- retain unused Sepolia ETH only in disposable test addresses;
- accept that failed execution still spends transaction or bundler fees.

No faucet request or funding command was run.
